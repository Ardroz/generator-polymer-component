language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-polymer-component' ]; then cd .. && eval "mv $currentfolder generator-polymer-component" && cd generator-polymer-component; fi
